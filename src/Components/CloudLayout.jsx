/* CloudLayout is no longer used — replaced by the global theme system.
   This stub prevents import errors from any stale references. */
export default function CloudLayout({ children }) {
  return <>{children}</>;
}
